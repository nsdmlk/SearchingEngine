# Поисковый движок (Search Engine)

Spring Boot-приложение для полнотекстового поиска по веб-сайтам с использованием индексации и лемматизации.

---

## 📌 Описание проекта

Поисковый движок — это локальное веб-приложение, которое:

- Автоматически обходит заданные сайты и индексирует их страницы
- Приводит слова к нормальной форме (лемматизация) для учёта морфологии русского языка
- Предоставляет REST API для управления индексацией и выполнения поисковых запросов
- Имеет веб-интерфейс с тремя вкладками: Dashboard, Management, Search

Проект разработан в рамках итогового задания курса **«Java-разработчик с нуля»**.

---

## 🛠 Технологический стек

- **Java 17+**
- **Spring Boot 2.7+**
- **MySQL 8.0+**
- **Hibernate / JPA**
- **Maven**
- **Jsoup** (парсинг HTML)
- **Lucene Morphology** (лемматизация)
- **Thymeleaf** (веб-интерфейс)
- **Fork-Join Framework** (многопоточный обход страниц)

---

## 🗂 Структура базы данных

В проекте используются 4 основные таблицы:

### `site`

- `id`, `url`, `name`
- `status` (`INDEXING`, `INDEXED`, `FAILED`)
- `status_time`, `last_error`

### `page`

- `id`, `site_id`, `path`, `code`, `content`

### `lemma`

- `id`, `site_id`, `lemma`, `frequency`

### `index`

- `id`, `page_id`, `lemma_id`, `rank`

---

## 🌐 Веб-интерфейс

Интерфейс состоит из трёх вкладок:

1. **Dashboard** — общая статистика по сайтам и процессам индексации
2. **Management** — управление индексацией (запуск, остановка, обновление отдельных страниц)
3. **Search** — поле для тестирования поиска с фильтрацией по сайту

Все данные загружаются через REST API.

---

## 🔌 REST API

### `GET /api/startIndexing`

Запуск полной индексации всех сайтов.

### `GET /api/stopIndexing`

Остановка текущей индексации.

### `POST /api/indexPage`

Добавление или обновление отдельной страницы по URL.

### `GET /api/statistics`

Получение статистики по индексации и сайтам.

### `GET /api/search`

Поиск по индексированным страницам с параметрами:

- `query` — поисковый запрос
- `site` — опционально, сайт для поиска
- `offset`, `limit` — пагинация

---

## 🚀 Локальный запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/ваш-логин/search-engine.git
cd search-engine
```
